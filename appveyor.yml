version: "{build}"

install:
  - choco install svn
  - gem install dev_tasks --version 1.0.28 --quiet --no-ri --no-rdoc
  
build: off

test_script:
  - set
  - svn help
  - rake

branches:
  only:
  - master
skip_tags: true
  
environment:
  DEV_ROOT: 'C:'
  matrix:
    - ruby_version: "193"
    - ruby_version: "220"
    - ruby_version: "220-x64"